# Step 13 — Polish & Équilibrage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter des animations de tir/mort/pose, des effets visuels d'impact (Canon, Mage Glace), améliorer l'éclair Foudre, ajouter un flash rouge à la base, et équilibrer les récompenses/intervalles de vagues.

**Architecture:** Approche A — tout inline dans les classes existantes. Fonctions helper locales dans chaque fichier. Aucun nouveau fichier système.

**Tech Stack:** Phaser 3, Vite, JavaScript ES modules. Pas de framework de test — vérification manuelle dans le navigateur (`npm run dev`).

---

## Fichiers modifiés

- `src/entities/Enemy.js` — effet de mort (cercle expand+fade)
- `src/entities/Tower.js` — animation de pose (scale-in) + flash de tir
- `src/entities/Projectile.js` — explosion Canon + anneau gel Mage Glace
- `src/scenes/GameScene.js` — flash rouge écran quand la base perd des PV + amélioration `fireLightning`
- `src/data/enemies.js` — ajustement rewards goblin/troll
- `src/data/waves.js` — ajustement intervalles vagues 5, 14, 17

---

### Task 1 : Effet de mort d'ennemi

**Files:**
- Modify: `src/entities/Enemy.js`

- [ ] **Étape 1 : Ajouter la fonction `playDeathEffect` en tête de fichier**

Dans `src/entities/Enemy.js`, juste avant la déclaration de classe, ajouter :

```js
function playDeathEffect(scene, x, y, color) {
  const g = scene.add.graphics().setDepth(7);
  g.fillStyle(color, 0.8);
  g.fillCircle(x, y, 14);

  scene.tweens.add({
    targets: g,
    scaleX: 2.5,
    scaleY: 2.5,
    alpha: 0,
    duration: 200,
    ease: 'Sine.easeOut',
    onComplete: () => g.destroy(),
  });
}
```

- [ ] **Étape 2 : Appeler `playDeathEffect` dans `Enemy.die()`**

Remplacer la méthode `die()` existante :

```js
die() {
  this.alive = false;
  playDeathEffect(this.scene, this.x, this.y, this.config.color);
  this.cleanup();
  this._onDie(this.reward, this.config);
}
```

- [ ] **Étape 3 : Vérifier dans le navigateur**

Lancer `npm run dev`. Tuer des ennemis — chaque mort doit produire un cercle coloré qui s'élargit et disparaît en 200ms.

---

### Task 2 : Animation de pose de tour

**Files:**
- Modify: `src/entities/Tower.js`

- [ ] **Étape 1 : Ajouter `playPlaceEffect()` comme méthode de Tower**

Dans la classe `Tower`, ajouter après le constructor :

```js
playPlaceEffect() {
  this.body.setScale(0);
  this.label.setScale(0);
  this.scene.tweens.add({
    targets: [this.body, this.label],
    scaleX: 1.2,
    scaleY: 1.2,
    duration: 160,
    ease: 'Back.easeOut',
    onComplete: () => {
      this.scene.tweens.add({
        targets: [this.body, this.label],
        scaleX: 1,
        scaleY: 1,
        duration: 120,
        ease: 'Sine.easeIn',
      });
    },
  });
}
```

- [ ] **Étape 2 : Appeler `playPlaceEffect()` à la fin du constructor de Tower**

À la toute fin du `constructor`, juste avant la fermeture `}`, ajouter :

```js
this.playPlaceEffect();
```

- [ ] **Étape 3 : Vérifier dans le navigateur**

Poser une tour — elle doit apparaître avec un effet pop (0→120%→100%) au lieu de surgir instantanément.

---

### Task 3 : Flash de tir sur les tours

**Files:**
- Modify: `src/entities/Tower.js`

- [ ] **Étape 1 : Ajouter `playFireEffect()` comme méthode de Tower**

Dans la classe `Tower`, ajouter :

```js
playFireEffect() {
  this.scene.tweens.add({
    targets: [this.body, this.label],
    scaleX: 1.35,
    scaleY: 1.35,
    duration: 60,
    ease: 'Sine.easeOut',
    yoyo: true,
  });
}
```

- [ ] **Étape 2 : Appeler `playFireEffect()` dans `fire()`**

Modifier la méthode `fire()` de Tower :

```js
fire(target, enemies) {
  this.playFireEffect();
  if (this.stats.chainCount) {
    this.fireLightning(target, enemies);
  } else {
    const getEnemies = () => this.scene.waveManager?.getEnemies() ?? [];
    new Projectile(
      this.scene, this.x, this.y,
      target,
      this.stats.damage,
      this.stats.splashRadius ?? 0,
      getEnemies,
      this.stats.damageType ?? 'physical',
      this.stats.slowFactor ?? 0,
      this.stats.slowDuration ?? 2000,
    );
  }
}
```

- [ ] **Étape 3 : Vérifier dans le navigateur**

Laisser les tours tirer — chaque tir doit produire un micro-flash de scale sur le corps de la tour.

---

### Task 4 : Explosion Canon à l'impact

**Files:**
- Modify: `src/entities/Projectile.js`

- [ ] **Étape 1 : Ajouter `playCanonExplosion` en tête de fichier**

Dans `src/entities/Projectile.js`, avant la déclaration de classe :

```js
function playCanonExplosion(scene, x, y, radius) {
  const g = scene.add.graphics().setDepth(5);

  // Anneau principal
  g.lineStyle(4, 0xf97316, 0.9);
  g.strokeCircle(x, y, 8);

  // 6 éclats rayonnants
  for (let i = 0; i < 6; i++) {
    const angle = (i / 6) * Math.PI * 2;
    const ex = x + Math.cos(angle) * 12;
    const ey = y + Math.sin(angle) * 12;
    const tx = x + Math.cos(angle) * (radius * 0.6);
    const ty = y + Math.sin(angle) * (radius * 0.6);
    const shard = scene.add.graphics().setDepth(5);
    shard.lineStyle(2, 0xfbbf24, 0.85);
    shard.beginPath();
    shard.moveTo(ex, ey);
    shard.lineTo(tx, ty);
    shard.strokePath();
    scene.tweens.add({
      targets: shard,
      alpha: 0,
      duration: 280,
      ease: 'Sine.easeIn',
      onComplete: () => shard.destroy(),
    });
  }

  scene.tweens.add({
    targets: g,
    scaleX: radius / 8,
    scaleY: radius / 8,
    alpha: 0,
    duration: 300,
    ease: 'Sine.easeOut',
    onComplete: () => g.destroy(),
  });
}
```

- [ ] **Étape 2 : Appeler `playCanonExplosion` dans `impact()` pour le Canon**

Modifier la méthode `impact()` de Projectile :

```js
impact() {
  const px = this.body.x;
  const py = this.body.y;
  const scene = this.body.scene;

  if (this.splashRadius > 0) {
    if (this.damageType === 'physical') {
      playCanonExplosion(scene, px, py, this.splashRadius);
    }
    this.getEnemies().forEach(e => {
      if (Phaser.Math.Distance.Between(px, py, e.x, e.y) <= this.splashRadius) {
        e.takeDamage(this.damage, this.damageType);
        if (this.slowFactor) e.applySlowEffect(this.slowFactor, this.slowDuration);
      }
    });
  } else {
    if (this.target.alive) {
      this.target.takeDamage(this.damage, this.damageType);
      if (this.slowFactor) this.target.applySlowEffect(this.slowFactor, this.slowDuration);
    }
  }
  this.body.destroy();
}
```

- [ ] **Étape 3 : Vérifier dans le navigateur**

Poser un Canon et viser un groupe d'ennemis — l'impact doit produire une explosion orange avec éclats rayonnants.

---

### Task 5 : Anneau de gel Mage Glace à l'impact

**Files:**
- Modify: `src/entities/Projectile.js`

- [ ] **Étape 1 : Ajouter `playFreezeRing` en tête de fichier**

Dans `src/entities/Projectile.js`, après la fonction `playCanonExplosion` :

```js
function playFreezeRing(scene, x, y, radius) {
  const g = scene.add.graphics().setDepth(5);
  g.lineStyle(3, 0x67e8f9, 0.9);
  g.strokeCircle(x, y, 6);

  const fill = scene.add.graphics().setDepth(5);
  fill.fillStyle(0xa5f3fc, 0.25);
  fill.fillCircle(x, y, radius);

  scene.tweens.add({
    targets: g,
    scaleX: radius / 6,
    scaleY: radius / 6,
    alpha: 0,
    duration: 250,
    ease: 'Sine.easeOut',
    onComplete: () => g.destroy(),
  });
  scene.tweens.add({
    targets: fill,
    alpha: 0,
    duration: 350,
    ease: 'Sine.easeOut',
    onComplete: () => fill.destroy(),
  });
}
```

- [ ] **Étape 2 : Appeler `playFreezeRing` dans `impact()` pour le Mage Glace**

Dans la méthode `impact()` déjà modifiée, mettre à jour le bloc `splashRadius > 0` :

```js
if (this.splashRadius > 0) {
  if (this.damageType === 'physical') {
    playCanonExplosion(scene, px, py, this.splashRadius);
  } else if (this.damageType === 'magic' && this.slowFactor > 0) {
    playFreezeRing(scene, px, py, this.splashRadius);
  }
  this.getEnemies().forEach(e => {
    if (Phaser.Math.Distance.Between(px, py, e.x, e.y) <= this.splashRadius) {
      e.takeDamage(this.damage, this.damageType);
      if (this.slowFactor) e.applySlowEffect(this.slowFactor, this.slowDuration);
    }
  });
} else {
  if (this.target.alive) {
    this.target.takeDamage(this.damage, this.damageType);
    if (this.slowFactor) this.target.applySlowEffect(this.slowFactor, this.slowDuration);
  }
}
```

- [ ] **Étape 3 : Vérifier dans le navigateur**

Poser un Mage Glace — l'impact de ses projectiles doit produire un anneau cyan qui s'élargit et une zone remplie semi-transparente.

---

### Task 6 : Amélioration visuelle chaîne Foudre

**Files:**
- Modify: `src/entities/Tower.js`

- [ ] **Étape 1 : Améliorer `fireLightning()` dans Tower**

Remplacer la méthode `fireLightning()` existante par :

```js
fireLightning(primary, allEnemies) {
  const chainRange = this.stats.chainRange ?? 90;
  const hit  = new Set([primary]);
  const path = [{ x: this.x, y: this.y }];
  let current = primary;

  for (let i = 0; i < this.stats.chainCount; i++) {
    if (!current.alive) break;
    path.push({ x: current.x, y: current.y });
    current.takeDamage(this.stats.damage, this.stats.damageType);

    const next = allEnemies
      .filter(e => e.alive && !hit.has(e) &&
        Phaser.Math.Distance.Between(current.x, current.y, e.x, e.y) <= chainRange)
      .sort((a, b) =>
        Phaser.Math.Distance.Between(current.x, current.y, a.x, a.y) -
        Phaser.Math.Distance.Between(current.x, current.y, b.x, b.y)
      )[0];

    if (!next) break;
    hit.add(next);
    current = next;
  }

  // Trait principal
  const g = this.scene.add.graphics().setDepth(4);
  g.lineStyle(3, 0xfde68a, 1);
  for (let i = 0; i < path.length - 1; i++) {
    g.beginPath();
    g.moveTo(path[i].x, path[i].y);
    g.lineTo(path[i + 1].x, path[i + 1].y);
    g.strokePath();
  }

  // Éclat blanc sur chaque cible touchée
  path.slice(1).forEach(pt => {
    const spark = this.scene.add.graphics().setDepth(5);
    spark.fillStyle(0xffffff, 0.9);
    spark.fillCircle(pt.x, pt.y, 5);
    this.scene.tweens.add({
      targets: spark,
      scaleX: 2.5,
      scaleY: 2.5,
      alpha: 0,
      duration: 180,
      ease: 'Sine.easeOut',
      onComplete: () => spark.destroy(),
    });
  });

  this.scene.tweens.add({
    targets: g,
    alpha: 0,
    duration: 180,
    onComplete: () => g.destroy(),
  });
}
```

- [ ] **Étape 2 : Vérifier dans le navigateur**

Poser une tour Foudre — les éclairs doivent être légèrement plus épais (3px) et chaque cible touchée doit émettre un petit éclat blanc.

---

### Task 7 : Flash rouge quand la base perd des PV

**Files:**
- Modify: `src/scenes/GameScene.js`

- [ ] **Étape 1 : Ajouter `playBaseHitFlash()` comme méthode de GameScene**

Dans `GameScene`, ajouter la méthode :

```js
playBaseHitFlash() {
  const { width, height } = this.scale;
  const overlay = this.add.rectangle(width / 2, height / 2, width, height, 0xff0000, 0.35).setDepth(20);
  this.tweens.add({
    targets: overlay,
    alpha: 0,
    duration: 250,
    ease: 'Sine.easeOut',
    onComplete: () => overlay.destroy(),
  });
}
```

- [ ] **Étape 2 : Appeler `playBaseHitFlash()` dans `onBaseHit()`**

Ajouter l'appel au début de `onBaseHit()`, juste après `this.cameras.main.shake(...)` :

```js
onBaseHit() {
  this.waveHpLost = true;
  this.runStats.baseHpLost += 1;
  this.baseHp -= 1;
  this.registry.set('baseHp', this.baseHp);
  this.cameras.main.shake(120, 0.0018);
  this.playBaseHitFlash();
  // ... reste inchangé
```

- [ ] **Étape 3 : Vérifier dans le navigateur**

Laisser un ennemi atteindre la base — l'écran doit flasher brièvement en rouge en plus du shake de caméra existant.

---

### Task 8 : Équilibrage ennemis

**Files:**
- Modify: `src/data/enemies.js`

- [ ] **Étape 1 : Ajuster rewards goblin et troll**

Dans `src/data/enemies.js`, modifier :

```js
goblin: {
  name: 'Gobelin',
  hp: 50,
  speed: 120,
  reward: 6,       // était 5
  color: 0x84cc16,
},
```

```js
troll: {
  name: 'Troll',
  hp: 300,
  speed: 55,
  reward: 35,      // était 25
  regenDelay: 3000,
  regenAmount: 20,
  color: 0x78716c,
},
```

---

### Task 9 : Équilibrage intervalles de vagues

**Files:**
- Modify: `src/data/waves.js`

- [ ] **Étape 1 : Ajuster les intervalles des vagues 5, 14 et 17**

Dans `src/data/waves.js` :

```js
{ wave: 5,  enemies: [{ type: 'goblin', count: 10, interval: 900  }, { type: 'orc', count: 3, interval: 1800 }] },
```

```js
{ wave: 14, enemies: [{ type: 'goblin', count: 15, interval: 700  }, { type: 'orc', count: 5, interval: 1500 }, { type: 'sorcerer', count: 3, interval: 2000 }] },
```

```js
{ wave: 17, enemies: [{ type: 'goblin', count: 20, interval: 600  }, { type: 'orc', count: 8, interval: 1400 }] },
```

- [ ] **Étape 2 : Vérifier dans le navigateur**

Lancer les vagues 5, 14 et 17 — le flux d'ennemis doit être légèrement moins oppressant.

---

### Task 10 : Commit final + mise à jour ROADMAP

**Files:**
- Modify: `ROADMAP.md`

- [ ] **Étape 1 : Cocher toutes les cases de l'étape 13 dans ROADMAP.md**

Mettre à jour `ROADMAP.md` — section Étape 13 :

```markdown
- [x] Ajouter des animations : tir, mort ennemi, pose de tour
- [x] Ajouter des effets visuels : explosion Canon, gel Mage, chaîne Foudre
- [ ] Ajouter des sons : tirs, morts, vague lancée, game over (assets libres)
- [x] Équilibrer : tester les vagues 1 à 20+, ajuster coûts et stats
- [x] Ajouter un retour visuel quand la base perd des PV (flash rouge)
```

(La case sons reste décochée — pas de sons dans ce run.)

- [ ] **Étape 2 : Commit**

```bash
git add src/entities/Enemy.js src/entities/Tower.js src/entities/Projectile.js src/scenes/GameScene.js src/data/enemies.js src/data/waves.js ROADMAP.md docs/superpowers/specs/2026-05-03-step13-polish-design.md docs/superpowers/plans/2026-05-03-step13-polish.md
git commit -m "polish: animations, visual effects and balance pass"
```
